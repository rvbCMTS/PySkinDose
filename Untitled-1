
class Exam:
    irradiation_events: List[IrradiationEvent] = []
    def __init__(rdsr_file: Optional[str] = None):

        ...
        if rdsr_file is not None:
            rdsr_data = _parse_rdsr(rdsr_file)


    def add_rdsr_data(rdsr_data_json: str):
        rdsr_data = <parse rdsr json>

    def _parse_rdsr(rdsr_file: str):
        dcm = pydicom.dcmread()
    
    def _rdsr_data_to_irradiation_events(rdsr_data: pd.DataFrame):
        for row in rdsr_data.iloc()?:
            self.irradiation_events.append(IrradiationEvent(row))
            